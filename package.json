{
  "name": "finch-utils",
  "version": "1.0.0",
  "description": "A sass utilities library",
  "main": "index.scss",
  "engines": {
    "node": "17.3.1"
  },
  "scripts": {
    "build": "rm -rf build && sass src:build",
    "build:demo": "rm -rf demo/styles && sass demo/scss:demo/styles && http-server -c-1 demo"
  },
  "keywords": [
    "sass",
    "scss",
    "css",
    "styles",
    "stylesheets",
    "utilities",
    "mixins"
  ],
  "author": "stephen pal-george",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "sass": "^1.55.0"
  }
}
